{
  "schema_version": 1,
  "about": "Declares what it takes to move between released versions of DockVault. Every release must appear here before it can be cut; the release workflow refuses otherwise. Edges are declared between ADJACENT releases only -- a longer upgrade is composed by walking them in order, rather than being asserted as a single hop nobody tested.",
  "kinds": {
    "direct": "The upgrade can be taken in one step. Any caveats are listed in the edge's conditions.",
    "blocked": "The upgrade must not be taken. The edge says why, and what to do instead."
  },
  "versions": {
    "0.6.0": {
      "released": "2026-07-21",
      "notes": "First published release."
    },
    "0.7.0": {
      "released": "2026-07-22",
      "notes": "No schema change."
    },
    "0.7.1": {
      "released": "2026-07-22",
      "notes": "No schema change."
    },
    "0.7.2": {
      "released": "2026-07-22",
      "notes": "No schema change."
    },
    "0.8.0": {
      "released": "2026-07-23",
      "notes": "No schema change."
    },
    "0.9.0": {
      "released": "2026-07-24",
      "notes": "No schema change."
    },
    "0.10.0": {
      "released": "2026-08-02",
      "notes": "No schema change. Every release up to here shares one physical schema: the set of boot DDL statements is byte-identical across all of them, and the model differs only in comment text."
    },
    "0.11.0": {
      "released": "2026-08-20",
      "notes": "First release to change the physical schema: adds the vault_storage_grants table and a nullable users.storage_quota_bytes column, applied automatically and idempotently at boot. The additions are unused by 0.10.0, so a rollback is clean; take a backup before upgrading."
    },
    "0.11.1": {
      "released": "2026-08-21",
      "notes": "No schema change; same physical schema as 0.11.0. Security patch: user-controlled values (the failed-login redis key and the alert resolver) are sanitized before they reach the security-monitor logs (log-injection hardening). A rollback is clean and needs no backup."
    },
    "0.11.2": {
      "released": "2026-08-21",
      "notes": "No schema change; same physical schema as 0.11.0/0.11.1. Security hardening: closes a temporary-credential mint vault-password oracle (vault membership is now verified before the password proof), audits admin-plane and cross-tenant access denials, bounds client-supplied encrypted-name metadata, orders the user-update authorization check before the existence lookup, and re-checks temporary-credential deactivation on the web path every request. A rollback is clean and needs no backup."
    },
    "0.12.0": {
      "released": "2026-08-22",
      "notes": "No schema change; same physical schema as 0.11.0/0.11.1/0.11.2. Adds credential lock/unlock/change-passphrase plus start/stop/restart/status lifecycle commands to the dockvault.py host management tool, so an operator can seal the .env secrets file into an encrypted .env.enc while a deployment is off (envelope encryption: a Fernet data key wrapped under a scrypt-derived passphrase key, with a stable recovery key). This is a host-tool change only; the container image is functionally unchanged from 0.11.2. Also hardens the test suite. A rollback is clean and needs no backup."
    },
    "0.13.0": {
      "released": "2026-08-23",
      "notes": "Account onboarding. Adds two tables (account_invitations, email_change_codes), created automatically and idempotently at boot by create_all; both are additive and unused by 0.12.0, so a rollback is clean -- take a backup before upgrading. Verified live: a 0.12.0 stack upgraded to 0.13.0 over the same volume creates both tables (schema complete) and self-signup works. Features: admin account invitations (mint / list / revoke, plus a public single-use accept link); public self-signup gated by an org policy (email requirement, signup domain allow/deny, login identifier username/email/either); self-service email change with an emailed one-time verification code; a last-admin-email lockout guard; and log-access fixes (web log-pull in the split-container shape plus a settings-tab overflow fix)."
    },
    "0.14.0": {
      "released": "2026-08-24",
      "notes": "Adds six additive tables (email_profiles, email_templates, email_resources for the Email Studio; notifications; notes; email_actions), all created automatically and idempotently at boot by create_all and unused by 0.13.0, so a rollback is clean -- take a backup before upgrading. Features: Email Studio (branded HTML email templates with grouped substitution tokens and automated-email actions that route the verification-code / password-reset / invitation emails through the template mechanism, with a fail-safe that never sends a system security email missing its code or link token); an in-app notification bell plus a reworked three-lane non-admin dashboard (What's waiting for you / Favourites / Most recent); move and copy for files and folders, within a vault and across Standard vaults (re-encrypting content under the destination binding; zero-knowledge cross-vault deferred); personal server-side notes with a snapshot 'send note' to other users; and a rate-limiter split giving per-chunk uploads and polled reads their own generous budgets so large uploads and normal polling are no longer throttled."
    },
    "0.15.0": {
      "released": "2026-08-25",
      "notes": "Email templates and automation (feature work landing across several changes; this note is extended as each lands). Schema so far: one additive nullable column, email_templates.default_key (created by create_all on a fresh DB, and by the boot DDL ADD COLUMN IF NOT EXISTS on an existing one), plus a partial-unique index over it. It marks a template as the built-in default for an automated-email action; boot seeds a default template per action and pre-binds each action to its default (idempotent, never overwriting an admin's own template or 'none' choice). The column is unused by 0.14.0 and the seed only adds rows, so a rollback is clean -- take a backup before upgrading. Later changes in this release build the automated-email delivery flows, a one-time-code (OTP) mechanism, and a password-reset flow on the same substrate; any additive tables those introduce are declared here when they land."
    }
  },
  "edges": [
    { "from": "0.6.0", "to": "0.7.0", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.7.0", "to": "0.7.1", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.7.1", "to": "0.7.2", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.7.2", "to": "0.8.0", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.8.0", "to": "0.9.0", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.9.0", "to": "0.10.0", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.10.0", "to": "0.11.0", "kind": "direct", "reversible": true, "requires_backup": true },
    { "from": "0.11.0", "to": "0.11.1", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.11.1", "to": "0.11.2", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.11.2", "to": "0.12.0", "kind": "direct", "reversible": true, "requires_backup": false },
    { "from": "0.12.0", "to": "0.13.0", "kind": "direct", "reversible": true, "requires_backup": true },
    { "from": "0.13.0", "to": "0.14.0", "kind": "direct", "reversible": true, "requires_backup": true },
    { "from": "0.14.0", "to": "0.15.0", "kind": "direct", "reversible": true, "requires_backup": true }
  ],
  "waivers": []
}
