# Test dependencies for the DockVault vault-service integration suite.
# These run on the HOST (not in the container) and exercise the live API/UI
# at http://localhost:8200.
#
# Install:  python -m venv .venv && .venv\Scripts\pip install -r requirements-test.txt
# Browsers: .venv\Scripts\playwright install chromium
pytest>=8.0,<9.0
requests>=2.31
playwright>=1.61.0
pytest-playwright>=0.5
websocket-client>=1.6
paramiko>=3.4  # SFTP client for the web<->SFTP round-trip tests
# Direct imports of test_master_password_limit.py and the modules it loads. They also arrive as
# paramiko transitives, but declaring them keeps the versions under test from drifting with
# paramiko's resolver — bcrypt in particular, whose >=5.0 behaviour that file asserts.
bcrypt>=5.0
cryptography>=44  # app/core/startup_security.py + scripts/setup_master_password.py
python-dotenv>=1.0  # imported by scripts/setup_master_password.py, which test_master_password_limit.py loads
